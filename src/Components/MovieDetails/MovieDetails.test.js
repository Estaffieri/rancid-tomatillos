import MovieDetails from './MovieDetails';
import { render, screen, waitFor } from "@testing-library/react";


describe('App', () => {
  it('should render movie details', () => {
    // should render movie details after a poster has been clicked

    //back button should fire goBackToMainPage function when clicked
  })
})
